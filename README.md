# CSV
This code defines a menu() function that displays a simple text-based menu. It prints options for the user, including reading and sorting numbers from a file, calculating the value of Pi, finding prime numbers, and exiting the program.
